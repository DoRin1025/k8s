apiVersion: v1
kind: Pod
metadata:
  name: my-web
  labels:
    name: myapp
spec:
  containers:
  - name: container-app
    image: httpd:latest
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: 80
